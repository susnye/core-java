[Unit]
Description=arrowhead-orchestrator
After=network.target mysql.target arrowhead-serviceregistry-sql.target

[Service]
WorkingDirectory=/etc/arrowhead/orchestrator
ExecStart=/usr/bin/java --add-modules java.xml.bind -jar /usr/share/arrowhead/systems/arrowhead-orchestrator-4.0.jar -d -daemon -tls
Type=simple
User=arrowhead
Group=arrowhead

[Install]
WantedBy=default.target
